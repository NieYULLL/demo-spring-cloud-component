<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">
    <dubbo:application name="order-consumer"/>
    <!--    group:会在zookeeper创建一个节点，消费者若要调用服务者，需要和服务者在同一group下-->
    <dubbo:registry group="user" address="zookeeper://127.0.0.1:2181"/>
    <dubbo:reference id="userService" check="false" interface="tech.nnoob.user.service.api.UserService"/>
</beans>